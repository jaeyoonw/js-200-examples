const obj = {
  //keys: values
  moive: "Sunny",
  music: "Like Sugar",
  style: "Retro",
  price: Infinity
};

const arr = Object.keys(obj);
console.log(arr);

const arr2 = Object.values(obj);
console.log(arr2);
